{
	"name": "crmgraphql",
	"version": "1.0.0",
	"description": "CRM using GraphQL and Apollo.",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Daniel Estupe",
	"license": "ISC",
	"dependencies": {
		"apollo-server": "^2.12.0"
	},
	"devDependencies": {
		"nodemon": "^2.0.3"
	}
}
